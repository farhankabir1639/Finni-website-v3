# Sanity CMS (required for Privacy Policy, Blog & Studio)
NEXT_PUBLIC_SANITY_PROJECT_ID=llw9afhn
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Shared secret for the Sanity → /api/revalidate webhook (instant blog publishing).
# Generate any random string and set the same value in your host env + the Sanity webhook URL.
SANITY_REVALIDATE_SECRET=
